Current:
Show path info in a readable format

Future:
Run gps collection in background (while device is asleep)
Add proper onstop/start state restoration techniques (support rotation)
Add multiple paths
Add named paths
Add path saving to disk
Add waypoints

